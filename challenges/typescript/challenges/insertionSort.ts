/**
 * This challenge is purposefully set up to have `insert` be solved first
 * */
export function insert(array: number[], rightIndex: number, value: number): void {

}

export function insertionSort(array: number[]): void {
  // how can you use insert to continuously insert values into their correct position?
}
